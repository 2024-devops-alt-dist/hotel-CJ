<nav class="navbar navbar-expand-lg navbar-light shadow-sm fixed-top">
    <div class="container-fluid mx-5">
        <a class="navbar-brand" href="#">
            <img src="assets2/images/logo_ts.png" alt="Logo" class="img-cust">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto gap-5 cust-padd">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_hotels_chambres')}}">Hôtels et Chambres</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact')}}">Contact</a>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto">
                <button class="btn mx-2 btn_search" id="" href="{{ path('app_hotels_chambres')}}">Réserver</button>
                {# {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="">Se déconnecter</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="">Se connecter</a>
                </li>
                {% endif %} #}
            </ul>
        </div>
    </div>
</nav>
